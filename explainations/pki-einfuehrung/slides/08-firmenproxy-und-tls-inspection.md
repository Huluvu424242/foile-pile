# Firmenproxy mit TLS-Inspection

![Firmenproxy trennt TLS](assets/04-proxy-split.svg)

Der Browser spricht nicht direkt mit der Webseite.  
Der Proxy beendet eine TLS-Verbindung und baut eine zweite TLS-Verbindung auf.
